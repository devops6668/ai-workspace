apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: k8s-ctl-state
  namespace: devops
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Mi
